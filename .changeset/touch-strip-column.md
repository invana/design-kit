---
"@invana/ui": minor
---

`TouchStrip` takes `orientation="column"` — one line per layer with the note right-aligned, the label
struck on a refusal and dimmed when nothing reached for it. It is how a 420px panel reads the same
*what it touched* summary a trace carries as a row of cells.
