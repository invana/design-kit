---
"@invana/themes": patch
---

`AppLayoutV2` borders its sections: the left section on its right edge, the right section on its
left edge and the main section on both, so the panels read as divided even where `--background`
and `--card` are close (the dark theme).
